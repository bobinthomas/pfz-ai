[build]
  base = "my-app"
  command = "npm run build"
  publish = "dist"

[build.environment]
  # Demo deploy: bundle fixtures until a live API is connected.
  VITE_USE_MOCKS = "true"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
